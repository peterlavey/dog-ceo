import {Box, Typography} from "@mui/material";

const NoMatches = ()=> {
    return (
        <Box>
            <Typography variant="h1" component="h2">
                No matches found
            </Typography>
        </Box>
    );
};

export default NoMatches;